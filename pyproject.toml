[project]
name = "pygraph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cytoolz>=1.1.0",
    "icecream>=2.1.10",
    "llvmlite==0.43.0",
    "numba==0.60.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
]
